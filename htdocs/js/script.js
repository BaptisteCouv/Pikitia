// function refreshChat() {
//     var refresh = document.getElementById('refresh0');

//     fetch('index.php#refresh0')
//         .then(function(response) {
//             return response.text();
//         })
//         .then(function(mytext) {
//             refresh.innerHTML = mytext;
//         });
// }

// setInterval("refreshChat()", 3000); // Fonctionne mais pas comme il faut.